# MyHealth

Health data ingestion API (Next.js + Supabase). Standalone Git repository — do not commit from the parent `myIdea` folder.

## Setup

```bash
npm install
# Create .env.local with SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, API_SECRET_KEY
npm run dev
```

## Init this repo only (separate from my-second-brain)

```bash
cd my-health
git init -b main
git add .
git commit -m "Initial commit"
git remote add origin <your-my-health-repo-url>
git push -u origin main
```
